server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tripMap?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: tripMap
    password: [REDACTED]
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: xieyongsen5@gmail.com
    password: [REDACTED]
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          timeout: 5000
          connectiontimeout: 5000
          writetimeout: 5000

# JWT閰嶇疆
jwt:
  secret: [REDACTED]
  expiration: 604800000  # 7澶╋紝鍗曚綅姣

logging:
  level:
    org.springframework.web: DEBUG