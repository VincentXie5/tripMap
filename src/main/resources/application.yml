server:
  port: 8080

spring:
  h2:
    console:
      enabled: true
      path: /h2-console
    database:
      url: jdbc:h2:mem:testdb
      driverClassName: org.h2.Driver
      username: sa
      password:

logging:
  level:
    org.springframework.web: DEBUG